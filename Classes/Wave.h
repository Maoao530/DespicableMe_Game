/*


#include "cocos2d.h"
#include "Creep.h"
#include "Enemy.h"

using namespace cocos2d;

class Wave : public Node{
public:
	Point pos ;
	//total 
	int totalCreeps;
	//
	double spawnRate;
	Creep* creepType;
	
	virtual bool init();
	CREATE_FUNC(Wave);
	Wave* initWithCreep(Creep* creep, double SpawnRate, int TotalCreeps);

public:
	Enemy* _enemyTtpe;
	int _enemyCount;
	void intiWithEnemy(Enemy* enemy,int enemyCount);
	
};

*/

